# P1-2A 发布清理

完成日期：2026-08-31。范围仅限 `skills/teaching-plan/` 公开 Skill 包；未修改根目录 `content/` 中的个人教案副本，未执行 Git 提交或推送。

## 已完成

- 移除默认模板和渲染回退中的学校、学期、日期事实。缺少这些输入时渲染为空白，不会填入具体学校或当前学年。
- 将两个公开示例的学校、班级、教师、学期和日期替换为明确的虚构示例值。
- 增加防回归测试，保证默认模板不会提供学校、学期或日期事实。
- 增加 `agents/openai.yaml`，提供公开安装后的显示名称、简介和默认调用提示。
- 将核心依赖限定为 `python-docx>=1.1.0,<2`，并在 `references/runtime_requirements.md` 区分核心依赖、目标编辑器验收与 LibreOffice 降级预览。
- 增加包内 `.gitignore`，忽略 Python 缓存；已删除发布包内现有 `__pycache__` 与 `.pyc` 文件。
- 完成 JSON、结构、质量与渲染回归：74 项自动化测试通过；`quick_validate.py` 通过。

## 发布前仍需用户决定

已按用户授权添加 MIT `LICENSE`（版权主体为 `teaching-plan contributors`）。该许可证只位于公开 Skill 包内，不覆盖仓库根目录 `content/` 中的个人教案副本。

## 不属于本阶段的后续工作

- P1-2B：完成 8 个评测用例的候选输出、双评审与仲裁。
- RC1：在 Windows Word 与 Windows WPS 进行代表性长教案全页验收。
