# P0 / P1-1 验收与 P1-2 进度

P0 验收日期：2026-08-28。P1-1 收口日期：2026-08-31。修改对象：`skills/teaching-plan/`。本轮没有覆盖根目录已有教案，也没有执行全局安装。

## 结论与范围

P0 与 P1-1 在默认模板、两个内置示例、本机 Microsoft Word for Mac，以及复杂 Linux 示例的 WPS Office for Mac 范围内通过。Skill 已具备内容校验、稳定编译、版面安全处理、技术文本排版和目标编辑器验收闭环。

这个结论不代表所有学校模板、所有 Word/WPS 版本或 Windows 环境已经通过，也不代表教学内容质量已经达到优秀水准。真实学校参考教案目前只纳入页面与表格几何回归，不能据此声称完整复刻或符合学校全部教务规范。

## P0 已完成

- 内容校验接入编译入口：必填字段、进度表列数、学时关系、新旧教学环节结构和输出文件名；错误阻断生成，待确认信息给出警告。
- 修正表格总宽、网格宽和合并单元格宽不一致的问题；基础信息、教学过程和反思区保持独立表格。
- 默认保留每课另起一页；需要自然接续时可配置 `pagination.lesson_start: flow`。
- Word/WPS 是目标版式的验收依据。LibreOffice 仅作明确标识的降级预览。
- 默认保留目标中文字体声明，不因生成机器缺少常用中文字体自动替换。

## P1-1 已完成

- 教学设计与设计意图按段落块进行无损、保序的高度平衡，减少短列造成的局部大空隙。
- 标题链与首段正文保持为原子块；短排版行禁止跨页，超过安全阈值的长行保留跨页能力，避免 Word 因 `cantSplit` 裁切超页内容。
- 教学过程拆分为独立表格片段，两行表头在普通续页重复显示；连续外框和片段底边保持闭合。
- 最后一条适合整体分页的教学行与反思区建立有限尾锚，避免反思单独落页；超长行不强制参与尾锚。
- 命令、参数、权限串、路径、IPv4/CIDR 使用可配置的技术文本字体，默认 `Courier New`；不插入不可见字符，不改变源文本。
- 包含技术文本的段落使用正确的 WordprocessingML 换行设置，避免 `/etc/passwd`、`rwxr-xr--`、`systemctl` 等 ASCII token 被按东亚文字逐字符拆行。
- 加入真实学校参考教案的 A4 页面、页边距、表格列宽、缩进和单元格边距回归测试。该测试只保证已提取几何，不代表完整模板保真。

## 自动化验证

- P1-1 收口时完整测试：60 项通过；进入 P1-2 后扩展为 73 项，仍全部通过。
- 覆盖内容校验、无损分块、列高平衡、标题关联、短行与超长行分页、重复表头、连续边框、反思尾锚、技术文本识别、字符保真、字体配置和参考文档几何。
- 官方 `skill-creator/scripts/quick_validate.py`：通过。校验器所需 `PyYAML 6.0.3` 安装在临时隔离目录，没有修改项目或全局 Python。

复跑命令：

```bash
python -m unittest discover -s skills/teaching-plan/tests -v
```

## 目标编辑器验收

### Microsoft Word

- 应用：Microsoft Word for Mac 16.112.2，构建号 16.112.26082125。
- 方法：直接控制 Word 打开新生成 DOCX，页数稳定后由 Word 原生导出本地 PDF，再逐页检查 PNG；没有通过 LibreOffice 转换，也没有保存 Word 提出的兼容性改写。

| 示例 | Word 页数 | 检查覆盖 | 结果 |
| --- | ---: | --- | --- |
| 影视后期制作，前四周 | 39 | 1–39 页 | 通过 |
| Linux 操作系统应用，前四周 | 27 | 1–27 页 | 通过 |

两个示例均未发现缺字、裁切、重叠、异常空白页、边框断裂、普通续页表头缺失或反思孤页。Linux 技术 token 防拆分检查通过。

### WPS Office

- 应用：WPS Office for Mac 12.1.28492，构建号 28492。
- 方法：直接控制 WPS 打开同一 Linux DOCX，通过 macOS 打印面板的“保存为 PDF”生成本地 PDF，再检查全部页面；未使用 WPS 云转换。

| 示例 | WPS 页数 | 检查覆盖 | 结果 |
| --- | ---: | --- | --- |
| Linux 操作系统应用，前四周 | 28 | 1–28 页 | 通过 |

WPS 28 页均未发现缺字、裁切、重叠、异常空白页、边框错位或反思孤页。普通教学过程续页均有“教学过程”和三列表头。`/etc/passwd`、`/etc/shadow`、`/usr/local/bin`、`rwxr-xr--`、`rw-r--r--`、`192.168.10.100`、`useradd`、`chmod`、`ifcfg-ens33` 等技术 token 未在内部误拆。

同一 Linux DOCX 在 Word 为 27 页，在 WPS 为 28 页。这是目标编辑器分页引擎差异；两套渲染均保持内容完整，不能以一方页数推定另一方。

## 本轮验收产物

本轮临时验收目录为 `/private/tmp/teaching-plan-p1-1-close.Lc6KVc/`，可能被系统自动清理，不作为正式交付目录。

```text
linux-final.docx
60e3040fe5a72ea1263837cfaa1e303b384febc5e300e62395915bc637cb1972

video-final.docx
a7377a85e3e456201985e8942fd5904ebb1ca868b8068db34c0008dbd09d2c99

wps-linux-final.pdf
3dbe3ecb22088d7900e241bcc063240f8825fc988b9d93d701b185722962a0ea
```

## 已知限制与未覆盖项

- 尚未在 Windows Word 或 Windows WPS 中验收，不把 macOS 结果推定为 Windows 通过。
- WPS 本轮选择内容更复杂的 Linux 示例完成全页覆盖；影视示例已在 Word 全页通过，但未额外做 WPS 全页覆盖。
- 单个物理表格行若本身高于一页，必须允许行内跨页。Word/WPS 对同一超长行内部的重复表头和边框处理能力有限，因此仍须逐页检查。
- 当前生成环境未发现样式中的微软雅黑，但 Word/WPS 实际渲染中文字完整。仍不保证不同教师电脑具有完全相同的字体文件和字形。
- 页眉页脚、文本框、图片、域、内容控件、复杂样式继承和多节页面等参考 DOCX 特性尚未完整提取。

## P1-2 教学质量评测进度

P1-1 在上述范围内正式收口。P1-2 已进入实施，当前不再优先增加版式规则，而是验证 Skill 是否真正“能备课”。详细基线与后续计划见 `P1-2评测基线.md`。

首批评测覆盖以下真实请求：

1. 材料不足，仅给出课程名与课时。
2. 用户明确给出不同的可用资源、设备限制或替代活动要求。
3. 45、90、135 分钟等不同课时长度。
4. 用户提供参考模板和学校字段要求。
5. 只修改已有教案，不允许整体重写。

统一量表检查：目标是否可观察，目标—活动—评价是否一一对应，教师和学生动作是否可执行，是否有明确验收物，时间是否可行，以及是否虚构教材、学情、设备或学校制度。材料不足时应追问、留空或明确假设，不以字段齐全代替教学质量合格。

P1-2 的首个交付物“评测用例集＋评分量表＋基线结果”已完成，并已根据真实失败项实施第一轮修正。下一步是保存 8 个用例的候选输出，完成双评审、分歧仲裁和回归评分；不得把任何单个评测用例的资源条件写成生成默认值。
